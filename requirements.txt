# Браузер (anti-detect). camoufox сам тянет playwright.
camoufox[geoip]>=0.4.11
playwright>=1.47.0

# Конфиги
PyYAML>=6.0.1

# Веб-интерфейс
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
